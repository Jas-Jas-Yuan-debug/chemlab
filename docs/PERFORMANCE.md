# 0.2 性能验证状态

**最终源码的完整性能验证尚未完成，不声明全场景达到 30 FPS。** 用户要求停止重复测试后，没有继续重跑。旧版 fae1e5b 的约十分钟、193 轮通过结果保留在 Git 历史中，不用于本次扩展。

硬件：Apple M4、16 GiB；Godot 4.7.2 / Metal Mobile。测试实际渲染图像为 1920×1080，采用 `frame_post_draw` 回调间隔；不是外部显示器捕获，也不采用无窗口计时。30 FPS 判据为 P99 ≤33.334 ms，且每个完整秒至少 30 帧；另要求足够回调样本。所有场景的原始记录和最长帧均保留。

## 首次独立 App 测量

[原始记录](../artifacts/performance-packaged.json) 来自最终保存修复之前的 0.2 App，阶段时长 3 秒、重复操作约 30 秒。该次测试未通过：

| 场景 | 平均 FPS | P99 / ms | 最低完整秒 FPS | 结果 |
|---|---:|---:|---:|---|
| 四个化学容器 | 38.86 | 271.73 | 45 | 样本不足，帧间隔未通过 |
| 十二容器与倾倒 | 25.33 | 105.59 | 18 | 帧率未通过 |
| 沉淀 | 37.65 | 253.54 | 46 | 样本不足，帧间隔未通过 |
| 新手区十二容器 | 21.40 | 251.68 | 0 | 样本不足，帧间隔未通过 |
| 新手加热，三维场关闭 | 67.53 | 17.69 | 60 | 本次短区间通过 |
| 新手加热，三维场开启 | 60.07 | 18.28 | 60 | 本次短区间通过 |

重复操作还暴露了切换到专业弹簧实验后保存仍保留新手加热视图的错误。已修复并通过定向新手保存恢复回归；不能因此推断所有性能问题已解决。上述失败的测量未完成包装器的最终 RSS/源码指纹写入，不能绑定到最新构建记录或据此给出新版峰值内存结论。

## 后续复测

测试工具补充了器材/材质预热，并改为显式检查每个场景的 30 FPS 判据。编辑器复测采用 6 秒阶段，运行到重复操作阶段时进程以 -15 结束，未写出最终逐帧结果，原因未确认；[状态记录](../artifacts/performance-editor-run.json) 明确标为中断，不能计为通过。

用户随后要求停止重复测试，因此未再启动编辑器或独立 App 性能复测。最新 App 已按当前源码重新打包，签名和内容身份见 [构建记录](../artifacts/build-receipt.json)；打包成功不等于性能验收。

原生科学模型、新手操作的已有验证与最终完整复测边界见 [扩展验证记录](../artifacts/expansion-validation.json) 和 [最近完整测试尝试](../artifacts/verification.json)。后者在 physics_flow 未收到通过标记后停止，未声称最终整套通过。
