# 性能与稳定性验证

当前版本的编辑器引擎测试已完整通过 16 个场景的 30 FPS 判据，实际图像为 1920×1080，编辑器窗口保持打开且最小化。数据见 `artifacts/performance-editor-run.json`。帧率是引擎渲染回调计时，不是外部显示器捕获。

打包程序的场景测量和约十分钟重复生命周期测试正在执行，尚未将此阶段标为完成。完成后由 `python3 scripts/report_performance.py` 从两份完整收据生成逐场景帧率、内存与响应报告。原始数据不由截图或无窗口运行推算。
